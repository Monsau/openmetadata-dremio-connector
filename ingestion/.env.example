# Variables d'environnement pour l'ingestion Dremio vers OpenMetadata
# ===================================================================
# Copier ce fichier en .env et renseigner les vraies valeurs

# Dremio Configuration
DREMIO_HOST=localhost
DREMIO_PORT=9047
DREMIO_USERNAME=admin
DREMIO_PASSWORD=admin123

# OpenMetadata Configuration
OPENMETADATA_HOST=localhost
OPENMETADATA_PORT=8585
OPENMETADATA_PROTOCOL=http

# OBLIGATOIRE : Token JWT OpenMetadata
# Récupérer depuis OpenMetadata UI → Settings → Bots → ingestion-bot → Token
OPENMETADATA_JWT_TOKEN=your_jwt_token_here

# Options avancées (optionnel)
# INGESTION_BATCH_SIZE=50
# INGESTION_TIMEOUT=60
# LOG_LEVEL=INFO